1. 指针与数组名
    A. 数组名是地址 (所以数组名不能修改)
    B. 指针的地址的地址

2. 指针操作数组-优先级
    A. *(p+1) = p[i]

3. 函数把数组形参转成指针，没法将数组本身传入函数
    A. 在数组声明的地方使用 sizeof 得到数组的实际大小
    B. 将数组名传入函数后使用 sizeof 得到的是指针本身的大小 (因为被数组名被转化为指针)

